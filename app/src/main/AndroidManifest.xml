<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.hardware.bluetooth" />
    <uses-feature android:name="android.software.companion_device_setup" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher2_round"
        android:supportsRtl="true"
        tools:targetApi="31">
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".CreateGroupActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".GroupListActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".PersonalMessageActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".BroadcastMessageActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".OptionsActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".AboutActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ChatActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".matrix_maeny.bluetoothchat.AboutActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".matrix_maeny.bluetoothchat.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".matrix_maeny.bluetoothchat.MainActivity2"
            android:theme="@style/AppTheme" />
    </application>

</manifest>